interface_version: 2
id: graphtyper2
name: GraphTyper2 external short-read SV genotyper
version: 2.7.7
source: external
paradigm: short_read_pangenome_genotyping
comparison_task: panel_genotyping

information_inputs:
  - target_short_reads
  - reference_grch38
  - canonical_candidate_panel
  - benchmark_panel_haplotypes
  - benchmark_panel_af_ac
  - benchmark_supplied_shared_alignment

information_contract:
  allowed_inputs_manifested: true
  target_truth_used_for_calling: false
  target_assembly_used: false
  target_family_genotypes_used: false
  target_specific_external_callset: false
  tuning_mode: official_recommended_sv_config
  parameter_source: official_documentation

tasks: [mapping, prepare_assets, genotyping, postprocess]

inputs:
  reference: required
  candidates: required
  pangenome_manifest: required
  pangenome_panel: required
  graph_assets:
    required: false
    accepted_formats: [other]
    accepted_profiles: []

capabilities:
  read_class: short
  technology: [illumina_pe]
  svtypes: [DEL, INS]
  phasing: false
  novel_discovery: false
  allele_linkage_output: benchmark_inferred
  allele_namespace: [PGSV]

native_assets:
  uses_population_or_pangenome_information: false
  frozen_haplotype_source_bundle: null

execution:
  runner: run.py
  environment: envs/environment.yaml
  rule_registry: rule-registry.yaml
  trust_level: untrusted
  sandbox_backend: bwrap

supported_modes:
  end_to_end_from_reads:
    required_inputs:
      - short_fastq_r1
      - short_fastq_r2
      - shared_shortread_alignment
      - shared_shortread_alignment_index
      - reference
      - reference_index
      - pangenome_manifest
      - pangenome_panel
      - candidate_panel
    optional_inputs: []
    billable_stages:
      - prepare_assets
      - genotype
      - postprocess

outputs:
  vcf: raw/calls.vcf
  candidate_output_contract: all_sites
  absence_semantics: no_call
