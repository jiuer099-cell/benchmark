channels:
  - conda-forge
  - bioconda
dependencies:
  - python=3.12
  # The graph.dist/minimizer/zipcodes bundle is format-coupled to vg.
  # Keep the runner on the exact version used to build the frozen indexes.
  - vg=1.73.0
